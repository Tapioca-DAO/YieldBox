1. `CRITICAL: [main] ERROR ALWAYS - Syntax error in spec file (sanity.spec:34:5): Cannot merge "YieldBoxHarness.getAssetTokenType(uint256) returns (uint8)" and "YieldBoxHarness.getAssetTokenType(uint256 assetId) returns (TokenType)" - they have incompatible return values: Cannot merge uint8 with TokenType`
We have nothing about enums in our doc or it's not related to CVL 2?

2. `CRITICAL: [main] ERROR ALWAYS - Syntax error in spec file (sanity.spec:126:5): could not type expression "DummyERC20A.balanceOf(e,0)", message: Failed to look up contract identifier "DummyERC20A" in symbol table (missing 'using' directive?).`
But the contract is on the scene. 
`using DummyERC20A as DummyERC20A;` helped.
Contract name is the same as file name.

the same for another contract: SimpleMintStrategy
```
rule strategyCorrelatesAsset(env e, env e2, method f) filtered {f -> excludeMethods(f)} {
    YieldBoxHarness.Asset asset;

    require asset.strategy == SimpleMintStrategy
        => (asset.tokenType == SimpleMintStrategy.tokenType(e) &&
            asset.contractAddress == SimpleMintStrategy.contractAddress(e) &&
            asset.tokenId == SimpleMintStrategy.tokenId(e));

    calldataarg args;
    f(e2, args);

    assert asset.strategy == SimpleMintStrategy
        => (asset.tokenType == SimpleMintStrategy.tokenType(e) &&
            asset.contractAddress == SimpleMintStrategy.contractAddress(e) &&
            asset.tokenId == SimpleMintStrategy.tokenId(e)), "Remember, with great power comes great responsibility.";
}
```
errors:
```
CRITICAL: [main] ERROR ALWAYS - Syntax error in spec file (sanity.spec:214:5): Variable SimpleMintStrategy is undefined (first instance only reported)
CRITICAL: [main] ERROR ALWAYS - Syntax error in spec file (sanity.spec:214:31): could not type expression "SimpleMintStrategy", message: unknown variable "SimpleMintStrategy"
CRITICAL: [main] ERROR ALWAYS - Syntax error in spec file (sanity.spec:215:32): could not type expression "SimpleMintStrategy.tokenType(e)", message: Failed to look up contract identifier "SimpleMintStrategy" in symbol table (missing 'using' directive?).
```



